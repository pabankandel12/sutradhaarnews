"use client";
import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import{Header}from"@/components/header";import{Footer}from"@/components/footer";import{browserApi}from"@/lib/api";
type Me={sub:string;email:string;role:string};
export default function ProfilePage(){const router=useRouter();const[me,setMe]=useState<Me|null>(null);useEffect(()=>{browserApi<Me>("/auth/me").then(setMe).catch(()=>router.replace("/login"))},[router]);if(!me)return <><Header/><main className="container inner-page"><div className="empty-state">खाता लोड हुँदैछ...</div></main><Footer/></>;return <><Header/><main className="container inner-page profile-page"><header><div className="profile-avatar">{me.email.slice(0,1).toUpperCase()}</div><div><span>पाठक खाता</span><h1>{me.email.split("@")[0]}</h1><p>{me.email}</p></div><button onClick={()=>{localStorage.removeItem("sutradhaar_reader_token");router.push("/")}}>लगआउट</button></header><div className="profile-grid"><section><h2>सुरक्षित समाचार</h2><div className="empty-state"><h3>अहिलेसम्म कुनै समाचार सुरक्षित छैन</h3></div></section><aside><h2>खाता</h2><a href="#">व्यक्तिगत विवरण</a><a href="#">मेरो प्रतिक्रिया</a><a href="#">सूचना सेटिङ</a></aside></div></main><Footer/></>}
