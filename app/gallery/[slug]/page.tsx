import { notFound } from "next/navigation"; import { Header } from "@/components/header"; import { Footer } from "@/components/footer"; import { publicApi } from "@/lib/api";
type Gallery={title:string;description?:string;photographer?:string;items:{id:string;caption?:string;media:{deliveryUrl?:string;url:string;title?:string}}[]};
export default async function GalleryPage({params}:{params:Promise<{slug:string}>}){const gallery=await publicApi<Gallery>(`/galleries/${(await params).slug}`).catch(()=>null);if(!gallery)notFound();return <><Header/><main className="container inner-page"><header className="archive-header"><span>फोटो ग्यालरी</span><h1>{gallery.title}</h1>{gallery.description&&<p>{gallery.description}</p>}</header><div className="related-grid">{gallery.items.map(item=><figure key={item.id}><img src={item.media.deliveryUrl||item.media.url} alt={item.caption||item.media.title||gallery.title}/>{item.caption&&<figcaption>{item.caption}</figcaption>}</figure>)}</div></main><Footer/></>}
