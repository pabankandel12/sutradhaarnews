import Link from "next/link"; import { Header } from "@/components/header"; import { Footer } from "@/components/footer"; import { publicApi } from "@/lib/api";
type Gallery={id:string;title:string;slug:string;description?:string;items:{media:{deliveryUrl?:string;url:string}}[]};
export default async function GalleryIndex(){const galleries=await publicApi<Gallery[]>("/galleries").catch(()=>[]);return <><Header/><main className="container inner-page"><header className="archive-header"><span>दृश्य समाचार</span><h1>फोटो ग्यालरी</h1><p>समाचारका तस्बिरहरू एकै ठाउँमा।</p></header>{galleries.length?<div className="related-grid">{galleries.map(g=><article key={g.id}><Link href={`/gallery/${g.slug}`}><img src={g.items[0]?.media.deliveryUrl||g.items[0]?.media.url||"/logo.jpg"} alt={g.title}/><h3>{g.title}</h3></Link></article>)}</div>:<div className="empty-state"><h2>ग्यालरी भेटिएन</h2></div>}</main><Footer/></>}
